# Addon wysiwyg-html-fieldtype — Etapy 1-4, 3b, 3c + wczesne hotfixy ✅ ZAMKNIĘTE

Data: 2026-06-01
Status: ✅ Wszystkie potwierdzone przez użytkownika

## Etap 1 — boilerplate

Namespace `Skalisty\WysiwygHtmlFieldtype`, package `skalisty/wysiwyg-html-fieldtype`, ServiceProvider.

## Etap 2 — PHP Fieldtype class

`augment()` → `HtmlString`, `configFieldItems()` (container, placeholder).

## Etap 3 — Vue component (IIFE)

TipTap 3 + CodeMirror 5, `resources/dist/addon.js` format IIFE, Vue externalizowane.

## Etap 3b — fix sync WYSIWYG↔HTML + min-height + link

Fix synchronizacji (`initCodeMirror` fallback bez CodeMirror), min-height edytora, przycisk linka 🔗, fullscreen CSS.

## Etap 3c — fullscreen Teleport + 7 nowych przycisków toolbara

`<Teleport :disabled="!isFullscreen" to="body">` naprawa fullscreen. Nowe przyciski: ← ↔ → ❝ ` ─ 🖼. CSS z `min-height: 0` na flex dzieciach.

## Etap 4 — integracja z Page Builderem

Free Text Section + nowy blok HTML Editor Block.

## Wczesne hotfixy

- HOTFIX nazwa: `wysiwyg-html-fieldtype` → `wysiwyg_html-fieldtype` (handle Vue component)
- HOTFIX symlink: `public/vendor/wysiwyg-html-fieldtype/js/addon.js` → symlink do `resources/dist/addon.js`
- HOTFIX IIFE walidacja: potwierdzono `format: 'iife'`, brak `@statamic/cms` w bundle — bez zmian kodu

## Free Text — opcja „Pełna szerokość bez ramki"

- `resources/fieldsets/free_text_section.yaml` — nowa opcja `full_no_padding`
- `resources/views/page_builder/free_text_section.antlers.html` — obsługa trzech przypadków width

## WYSIWYG — zaznaczanie obrazków

CSS w `WysiwygHtml.vue`: cursor pointer + `.ProseMirror-selectednode { outline }` przy zaznaczeniu.
